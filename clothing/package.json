{
  "name": "mineralx-clothing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Independent X by MineralX pre-launch clothing storefront.",
  "scripts": {
    "build": "node scripts/verify.mjs",
    "test": "node --test tests/*.test.mjs",
    "preview": "python3 -m http.server 8080 --directory dist"
  }
}
